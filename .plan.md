# CI triage/fix plan for `origin/main` (task `ci-main-red-20260515`)

## Rebase note

This file was resolved during the local rebase onto `origin/main`. Both sides described the same CI triage, with different levels of detail. The merged record below keeps the facts needed for audit while removing conflict markers.

## Safety context

- Source repo: `/home/main/dev/animaworks-bak`
- Source repo started dirty and behind `origin/main` by 2 commits.
- Existing local changes included runtime/config edits, taskboard CSS, planning/report files, and benchmark results.
- Earlier CI triage used a clean clone/worktree to avoid overwriting local edits.
- Destructive git cleanup commands remained prohibited throughout.

## Facts established

1. GitHub Actions runs `25867385771` and `25867027804` failed on `main` in `unit-tests` step `Lint`.
2. Those failures were Ruff violations in `core/memory/_activity_replay.py`:
   - `UP035`: import `Iterable` from `collections.abc`
   - `SIM114`: combine equivalent suppression branches with `or`
3. After the explicit Ruff fixes, `ruff format --check core/ cli/ server/` reported formatting changes in:
   - `core/memory/_activity_replay.py`
   - `core/memory/ontology/default.py`
   - `core/memory/priming/gate.py`
   - `core/skills/router.py`
   - `server/routes/taskboard.py`
4. Older run `25855874702` failed a unit test that expected warning threshold `5000`.
5. Latest `main` and the fixed test expectation use `injection_size_warning_chars = 2000`; rebase resolution keeps that runtime value to avoid reintroducing the historical failure.
6. Upstream commit `3dcdc178` already includes the main CI fixes for taskboard CSS parity and stale unit-test baselines.

## Files involved

- `core/memory/_activity_replay.py`: Ruff import/condition cleanup and formatting.
- `core/memory/ontology/default.py`: Ruff formatting.
- `core/memory/priming/gate.py`: Ruff formatting.
- `core/skills/router.py`: Ruff formatting and i18n baseline context.
- `server/routes/taskboard.py`: Ruff formatting.
- `server/static/styles/task-board.css`: taskboard active-state parity.
- `tests/unit/test_injection_size_governance.py`: stale threshold assertion updated upstream to `2000`.
- `tests/unit/test_i18n_hardcode.py`: router Japanese particle baseline updated upstream.

## Verification recorded during triage

- `ruff check core/ cli/ server/`: passed after `_activity_replay.py` fix.
- `ruff format --check core/ cli/ server/`: passed after formatting.
- `pytest tests/unit/test_injection_size_governance.py -q`: `4 passed`.
- `pytest tests/unit/frontend/test_responsive_design.py::TestHoverActiveParity::test_hover_active_parity_dashboard_css -q`: `1 passed`.
- `pytest tests/unit/test_i18n_hardcode.py -q`: `3 passed`.

## Current rebase resolution

- Preserve the local runtime investigation artifacts and benchmark outputs.
- Preserve upstream CI fixes from `origin/main`.
- Resolve `_suppressed_count()` with explicit grouping so Ruff and operator precedence are clear.
- Keep prompt injection warning threshold at `2000` to match current tests and upstream runtime behavior.

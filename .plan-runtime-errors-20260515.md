# Runtime error investigation plan — runtime-errors-20260515

## Goal
Classify the non-busy-hang runtime errors seen in `/home/main/.animaworks/logs/animaworks.log` during the observed window, and determine whether each pattern is:
- a real production issue,
- a known/expected test artifact or false positive, or
- an unresolved defect needing a fix proposal.

## Scope
Investigate at minimum these patterns:
1. `Mode B preflight: prompt too large (~127700 tokens input, 128000 window)`
2. `Bootstrap failed/timed out for kotoha`
3. `[alice] run_heartbeat FAILED`

Also account for adjacent lines that may explain provenance, including:
- `Config file not found at /tmp/.../config.json; using defaults`
- `Context approaching limit ... test-autocompact`
- `Failed to set up anima 'sakura' from MD file; rolling back`
- `Timeout in streaming process_message`
- stack traces referencing `/tmp/aw-ci-main-red-20260515-clone/` or `/tmp/pytest-of-main/...`

## Pre-read evidence already gathered
- The `alice` failures are surrounded by `DigitalAnima 'alice' initialized from /tmp/pytest-of-main/...` lines, strongly suggesting pytest-only fixtures.
- The `kotoha` bootstrap failures include stack traces from `/tmp/aw-ci-main-red-20260515-clone/...` and `unittest.mock` exceptions (`IPC broken`, `fail`), suggesting unit/e2e test execution rather than live supervisor failures.
- The prompt-too-large line appears next to `Context approaching limit` messages and `test-autocompact`, suggesting context-window tests may be the source.

## Required machine steps
1. Re-read the exact log window and extract all relevant surrounding lines for the three target patterns.
2. Search the repository for tests that intentionally emit these error paths.
3. Identify the concrete test files and assertions that explain each pattern, if any.
4. Distinguish production-path code behavior from test-only mocked behavior.
5. Produce a concise investigation report with, for each pattern:
   - known vs unknown classification
   - exact evidence (log lines, file paths, test names, code paths)
   - trigger/repro condition
   - impact assessment
   - fix proposal only if the issue is not merely test-derived / expected
6. State whether immediate escalation is needed; only recommend escalation if there is evidence of active production data loss or a security issue.

## Constraints
- Do not edit code unless strictly necessary; this task is primarily investigative.
- Do not treat test-generated failures as new incidents without direct evidence.
- Be explicit when inferring from evidence.
- Use `/home/main/dev/animaworks-bak` as the working directory.

## Expected output format
Return a Markdown report with sections:
- `## Findings summary`
- `## Pattern 1: prompt too large`
- `## Pattern 2: kotoha bootstrap`
- `## Pattern 3: alice run_heartbeat`
- `## Escalation judgment`
- `## Recommended next actions`

For each pattern, include concrete log evidence and repository references.

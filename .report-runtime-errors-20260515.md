# Runtime error investigation report — runtime-errors-20260515

## Method
- Read plan file `/home/main/dev/animaworks-bak/.plan-runtime-errors-20260515.md`
- Re-verified log lines in `/home/main/.animaworks/logs/animaworks.log`
- Re-verified production code paths and matching tests in the working tree
- All evidence below was checked in this run (not relayed from a prior report)

## Findings summary
All three target patterns are **test-derived log artifacts**, not live production incidents:

| Pattern | Verdict | Production caller resilience |
|---------|---------|------------------------------|
| 1. Mode B preflight prompt too large | Test-derived | Caller returns explicit `[Error: prompt too large …]` text; no crash |
| 2. Bootstrap failed/timed out for kotoha | Test-derived | Production retry/rename logic is exactly the path under test |
| 3. `[alice] run_heartbeat FAILED` | Test-derived | `_handle_heartbeat_failure` recovery path is what tests exercise |

All three patterns repeat in three tight clusters within `2026-05-14T17:04Z–17:18Z`, which is consistent with three pytest runs (or one CI run that re-executes the same suites). No production-Anima provenance was found for any of the three patterns. **Immediate escalation is unnecessary.**

## Pattern 1: `Mode B preflight: prompt too large (~127700 tokens input, 128000 window)`
- **Classification:** known / test-derived
- **Log evidence:**
  - `animaworks.log:47` (2026-05-14T17:04:12.002005Z)
  - `animaworks.log:15822` (2026-05-14T17:09:39.123936Z)
  - `animaworks.log:31591` (2026-05-14T17:15:53.906175Z)
- **Adjacent provenance:** each occurrence is interleaved with `Mode B preflight: clamping max_tokens 4096 -> 1872 (est_input ~126000, window 128000)` from the same `animaworks.execution.assisted` logger — the 126000 (clamp branch) and 127700 (over-window branch) constants match unit-test fixtures exactly.
- **Production code:** `core/execution/assisted.py:288-292` (logger.error call inside Mode B preflight)
- **Matching tests:**
  - `tests/unit/core/execution/test_assisted.py:396` — `patch("litellm.token_counter", return_value=126000)` → clamp branch (`128000-126000=2000`, clamped to `2000-128=1872`)
  - `tests/unit/core/execution/test_assisted.py:408` — `patch("litellm.token_counter", return_value=127700)` → over-window branch (logs the error line)
  - Adjacent suites: `tests/unit/core/execution/test_context_autocompact.py`, `tests/e2e/test_context_autocompact_e2e.py`
- **Trigger/repro:** pytest exercises preflight token-budget branches with hardcoded `gemma3` fallback `context_window=128000`.
- **Production impact:** none. The `return None` at `assisted.py:293` causes the caller (`assisted.py:456` / `_litellm_streaming.py:172,812` / `litellm_loop.py:194`) to emit a graceful `[Error: prompt too large for <model>]` response rather than crashing.
- **Fix proposal:** none required. Optional: downgrade the log entry from `ERROR` to `WARNING` to reduce alert-noise weight, since the caller already presents a user-facing error message.

## Pattern 2: `Bootstrap failed/timed out for kotoha`
- **Classification:** known / test-derived
- **Log evidence (cluster 1, 17:06:36Z):** `animaworks.log:4194-4229`
  - `4195`: `Bootstrap failed for kotoha: failed` (response.error path)
  - `4202`, `4205`, `4222`, `4225`: `Bootstrap retry limit reached … Renamed bootstrap.md -> bootstrap.md.failed`
  - `4209`: `Bootstrap timed out for kotoha (600s)` (TimeoutError path)
  - `4213`: `Bootstrap error for kotoha` with full traceback — frames reference `/tmp/aw-ci-main-red-20260515-clone/core/supervisor/manager.py` and `unittest/mock.py`, raising `RuntimeError: IPC broken`
  - `4229`: same shape, raising `RuntimeError: fail`
  - `4211`,`4215`,`4224`,…: `Config file not found at /tmp/tmpeb3nho87/config.json; using defaults` confirms pytest tmp dirs
- **Log evidence (cluster 2, 17:11:51Z):** `animaworks.log:19944-20000+` (same pattern repeats)
- **Production code:**
  - `core/supervisor/manager.py:380-393` — retry-limit log + rename
  - `core/supervisor/manager.py:432-435` — `Bootstrap failed for %s: %s`
  - `core/supervisor/manager.py:456` — `Bootstrap timed out for %s (600s)`
  - `core/supervisor/manager.py:461-462` — `Bootstrap error for %s` (logger.exception)
- **Matching tests:** `tests/unit/core/supervisor/test_bootstrap_protection.py`
  - `:237-244` — `mock_response.error = {"message": "failed"}` → produces `Bootstrap failed for kotoha: failed`
  - `:274-285` — verifies `bootstrap.md.failed` rename after `_bootstrap_max_retries` is reached
  - `:307` — `side_effect=asyncio.TimeoutError` → produces `Bootstrap timed out for kotoha (600s)`
  - `:321` — `side_effect=RuntimeError("IPC broken")` → exact traceback at log:4213
  - `:329-348` — drives multiple failures to exceed retry limit and trigger rename
  - `:371` — `side_effect=RuntimeError("fail")` → exact traceback at log:4229
- **Trigger/repro:** these test fixtures.
- **Production impact:** none. No live `kotoha` heartbeat log under `~/.animaworks/logs/animas/kotoha/` for 2026-05-14 is implicated; all kotoha-bootstrap error lines come from pytest tmp dirs.
- **Fix proposal:** none required.

## Pattern 3: `[alice] run_heartbeat FAILED`
- **Classification:** known / test-derived
- **Log evidence:** three clusters, one per pytest run:
  - `animaworks.log:5528,5533,5537,5541,5545,5549,5554` (17:06:50Z, `pytest-13`)
  - `animaworks.log:21299,21304,21308,21312,21316,21320,21325` (17:12:03Z, `pytest-17`)
  - `animaworks.log:37068,37073,37077,37081,37085,37089,37094` (17:18:18Z, `pytest-19`)
- **Key provenance:** every failure line is immediately preceded by `DigitalAnima 'alice' initialized from /tmp/pytest-of-main/pytest-{13,17,19}/test_<name>0/.animaworks/animas/alice`. There is no production alice anima.
- **Production code:** `core/_anima_heartbeat.py:648` — `logger.exception("[%s] run_heartbeat FAILED", self.name)` inside `_handle_heartbeat_failure`
- **Matching tests:** `tests/unit/core/test_anima_heartbeat_decomposition.py`
  - `:1124` class `TestHandleHeartbeatFailure` with the seven test methods whose names map 1:1 to the log paths:
    - `:1125` `test_crash_archive_inbox_messages` → `test_crash_archive_inbox_messa0`
    - `:1144` `test_empty_inbox_no_crash_archive` → `test_empty_inbox_no_crash_arch0`
    - `:1161` `test_recovery_note_written` → `test_recovery_note_written0`
    - `:1182` `test_error_activity_logged` → `test_error_activity_logged0`
    - `:1206` `test_no_relay_queue_no_error` → `test_no_relay_queue_no_error0`
    - `:1221` `test_crash_archive_failure_handled` → `test_crash_archive_failure_han0`
    - `:1243` `test_recovery_note_with_long_error_truncated` → `test_recovery_note_with_long_e0`
- **Trigger/repro:** pytest invokes `_handle_heartbeat_failure` directly with mocked exceptions to test recovery-path behavior (crash-archive, recovery-note, activity-log error entry).
- **Production impact:** none.
- **Fix proposal:** none required.

## Escalation judgment
**Do not escalate.** No production data loss, no security signal, no live-Anima bootstrap or heartbeat failures for the observed window. All three error patterns are emitted by code paths that pytest is intentionally driving with mocked failures, and the production callers handle each path defensively (rename + manual-intervention flag, graceful error response, recovery note).

## Recommended next actions
1. **No incident.** Do not page on these three patterns.
2. **Logging hygiene (optional):** isolate test runs so they do not write to the shared `~/.animaworks/logs/animaworks.log`. The simplest approach is for the test harness to set `ANIMAWORKS_LOG_DIR` (or equivalent) to a pytest tmp dir, so production log files are not polluted by mocked failures.
3. **Alerting hygiene (optional):** if log-based alerting watches `animaworks.log` for `ERROR` lines, add a suppression rule for entries whose surrounding context (within ~5 lines) includes `/tmp/pytest-of-main/`, `/tmp/aw-ci-main-red-`, or `unittest/mock.py` frames.
4. **Severity tuning (optional):** `Mode B preflight: prompt too large` could be downgraded from `ERROR` to `WARNING` (the caller already emits a user-facing error message), so this line stops triggering ERROR-level alerts in normal operation when a model legitimately overflows its window.
